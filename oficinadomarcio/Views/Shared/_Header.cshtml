<nav class="navbar navbar-expand-sm navbar-custom navbar-dark px-6">
    <div class="container-fluid">
        @Html.ActionLink("Oficina do Marcio", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
            <ul class="navbar-nav">
                @if (User.IsInRole("MECANICO") || User.IsInRole("MECANICO"))
                {
                    <li class="nav-item">
                        @Html.ActionLink("SERVIÇOS", "Index", "Servicos", null, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("AGENDAMENTOS", "Index", "Agendamentos", null, new { @class = "nav-link" })
                    </li>
                }
                @if (User.IsInRole("ADMIN"))
                {
                    <li class="nav-item">
                        @Html.ActionLink("ADMIN", "Admin", "Home", null, new { @class = "nav-link" })
                    </li>
                }
                @if (User.IsInRole("CLIENTE"))
                {
                    <li class="nav-item">
                        @Html.ActionLink("REGISTRE SEU VEÍCULO", "Create", "Veiculos", null, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("FAÇA SEU AGENDAMENTO", "Create", "Agendamentos", null, new { @class = "nav-link" })
                    </li>
                }

                @Html.Partial("_LoginPartial")
            </ul>
        </div>
    </div>
</nav>